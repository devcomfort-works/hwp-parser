name: Deploy to Hugging Face Spaces

on:
  push:
    branches: [main]
  workflow_dispatch:  # 수동 실행 가능

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Prepare deployment directory
        run: |
          mkdir deploy_dist
          
          # 1. 소스 코드 복사 (패키지 구조 변경)
          # src/hwp_parser를 루트의 hwp_parser로 복사하여 설치 없이 import 가능하게 함
          cp -r src/hwp_parser deploy_dist/hwp_parser
          
          # 2. app.py 배치
          cp src/hwp_parser/web/app.py deploy_dist/app.py
          
          # 3. requirements.txt 수동 생성 (설치 문제 우회)
          # pyproject.toml의 dependencies 항목을 그대로 옮김
          cat <<EOF > deploy_dist/requirements.txt
          pyhwp>=0.1b15
          html2text>=2025.4.15
          html-to-markdown>=2.23.1
          llama-index-core>=0.14.12
          click>=8.3.1
          gradio>=6.3.0
          loguru>=0.7.3
          EOF
          
          # 4. Space 전용 README.md 생성
          echo "---" > deploy_dist/README.md
          echo "title: HWP Parser" >> deploy_dist/README.md
          echo "emoji: 📄" >> deploy_dist/README.md
          echo "colorFrom: blue" >> deploy_dist/README.md
          echo "colorTo: indigo" >> deploy_dist/README.md
          echo "sdk: gradio" >> deploy_dist/README.md
          echo "sdk_version: 4.16.0" >> deploy_dist/README.md
          echo "app_file: app.py" >> deploy_dist/README.md
          echo "pinned: false" >> deploy_dist/README.md
          echo "---" >> deploy_dist/README.md
          echo "" >> deploy_dist/README.md
          # 원본 README 내용 이어붙이기
          cat README.md >> deploy_dist/README.md

      - name: Push to Hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          cd deploy_dist
          git init
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add .
          git commit -m "Deploy from GitHub Actions: ${{ github.sha }}"
          
          # HEAD:main 문법을 사용하여 로컬 브랜치 이름(master/main)에 상관없이 
          # 리모트의 'main' 브랜치로 강제 푸시합니다.
          # 다른 브랜치로 배포하려면 뒤의 'main'을 'gh-pages' 등으로 변경하세요.
          git push --force https://devcomfort:$HF_TOKEN@huggingface.co/spaces/devcomfort/hwp-parser HEAD:main
